@model TAS_SIUKDWStore.Models.Message

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_FluidLayout.cshtml";
}

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Message</legend>
       <p> @Html.DisplayFor(model => model.ContentMessage)</p>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        if (User.Identity.Name == @model.UserName)
        {
        <input type="submit" value="Delete" /> |
        }
        else
        {
        
        
        }
        
        <a href="javascript:void(0);" onclick="history.go(-1);"><button type="button" class="btn btn-sm btn-warning">Cancel</button></a>
    </p>
}

<script type="text/javascript">
    $(document).on('click', '#backLink', function () {
        history.go(-1);
    });
</script>